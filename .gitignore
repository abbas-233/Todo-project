# Ignore Node dependencies folder
node_modules/

# Ignore operating system files
.DS_Store
Thumbs.db

# Ignore log files and temporary files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
*.tmp

# Note: We are NOT ignoring the dist/ folder because
# for the simple GitHub Pages deployment method,
# we will commit the contents of dist/ directly.
# If using GitHub Actions for building, you would ignore dist/.

